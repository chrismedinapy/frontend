import { EmptyState } from '../../components/Ui'

export function AuthPage() {
  return <main className="standalone-page"><EmptyState title="Acceso en preparación" description="El módulo de autenticación se integra desde feature/auth." /></main>
}
